PORT=4000
DATABASE_URL=postgres://user:password@localhost:5432/dealhunter
ANTHROPIC_API_KEY=your_api_key_here

# How often the mock price feed ticks (ms). Keep low for demos.
PRICE_TICK_INTERVAL_MS=5000

# How often the agent evaluates active rules (ms).
AGENT_TICK_INTERVAL_MS=7000
